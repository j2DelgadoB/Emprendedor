<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jose.emprendedor" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <fragment android:name="LoginFragment" />

        <activity
            android:name=".Empresa"
            android:label="Selecciona Empresa" >
        </activity>

        <activity
            android:name="VentasTotales"
            android:label="Ventas totales">

        </activity>
        <activity
            android:name="VentasCategoria"
            android:label="Ventas x categoria">

        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
